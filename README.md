# Simple Python CLI To-Do List

A lightweight, interactive Command Line Interface (CLI) To-Do List application built with Python. This tool allows users to manage their daily tasks directly from the terminal. Tasks are persistently stored in a local JSON file so you never lose your progress.
